package telegramium.bots

/** Describes an inline message sent by a guest bot.
  *
  * @param inlineMessageId
  *   Identifier of the sent inline message
  */
final case class SentGuestMessage(inlineMessageId: String)
